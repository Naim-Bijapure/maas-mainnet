{
    "name": "backend",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "start": "yarn cp:bundle && node index.js",
        "deploy": "git add . && git commit -m Heroku && git push heroku master",
        "cp:bundle":"cp -rf ../react-app/build ."
    },
    "dependencies": {
        "body-parser": "^1.20.0",
        "cors": "^2.8.5",
        "express": "^4.18.0",
        "fs": "^0.0.1-security",
        "https": "^1.0.0"
    }
}
